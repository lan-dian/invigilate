spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/hfut?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=true
    username: hfut
    password: 123456


  servlet:
    multipart:
      # 开启文件上传功能
      enabled: true
      max-file-size: 100MB
      max-request-size: 100MB

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

token:
  privateKey: 'hfut'
